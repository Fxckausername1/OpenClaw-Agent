---
name: crm-sync
description: Sync lead rows to CRM systems (HubSpot, Pipedrive). Scaffold only — requires API keys and explicit user approval to send data.
user-invocable: true
---

Includes a safe sync stub. The script demonstrates mapping fields and performing dry-run mode before any live writes.

Included:
- scripts/sync_stub.mjs — dry-run sync CLI.

