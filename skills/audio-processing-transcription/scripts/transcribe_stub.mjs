#!/usr/bin/env node
// transcribe_stub: demonstrates calling a transcription model or service
const args = process.argv.slice(2);
console.log('transcribe_stub: args', args.join(' '));
console.log('Output example: {transcript: "...", timestamps:[{start,end,text}]}');

