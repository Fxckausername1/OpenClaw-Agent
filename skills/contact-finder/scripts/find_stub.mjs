#!/usr/bin/env node
// contact-finder stub
// Usage: node find_stub.mjs --name "Artist Name" --city "Atlanta"

const args = process.argv.slice(2);
console.log('contact-finder: stub run with', args.join(' '));
console.log('Output example: {name,emails:[...],phones:[...],sources:[url,...]}');

